/**
 * Public API for the contracts layer.
 *
 * Import anything contract-related from '@/lib/contracts' — never
 * reach into the individual modules directly from outside this folder.
 */
export { PROBABLE_MARKET_ABI } from './abi';
export { CONTRACT_ADDRESSES } from './addresses';
export { probableMarketConfig } from './probableMarket';
export type { ProbableMarketAddress } from './probableMarket';
